import { FlashcardSystem } from "@/components/features/flashcards/FlashcardSystem";

/**
 * Flashcards Page — Enterprise HSK 3.0 SRS System
 */
export default function FlashcardsPage() {
  return (
    <div className="max-w-6xl mx-auto px-4 sm:px-8 py-8 md:py-10 space-y-8 min-h-screen">
      <FlashcardSystem />
    </div>
  );
}
